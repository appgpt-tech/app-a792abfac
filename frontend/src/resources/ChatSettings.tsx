//Source code generated by AppGPT (www.appgpt.tech)
import {
  Datagrid,
  List,
  EditButton,
  Edit,
  SimpleForm,
  Create,
  SelectColumnsButton,
  DatagridConfigurable,
  TopToolbar,
  CreateButton,
  ExportButton,
  FilterButton,
  //Field controls
  BooleanField,
  DateField,
  EmailField,
  ImageField,
  NumberField,
  ReferenceField,
  TextField,
  UrlField,
  //Input controls
  BooleanInput,
  DateInput,
  EmailInput,
  ImageInput,
  NumberInput,
  ReferenceInput,
  TextInput,
  UrlInput,
  PasswordInput,
} from 'react-admin';
import { useRecordContext } from 'react-admin';
import { Grid } from '@mui/material';
const ReadOnlyPasswordField = ({ record, source }) => {
  // You can customize the way you display the password here, e.g., mask it with asterisks
  const maskedPassword = '********';

  return <span>{maskedPassword}</span>;
};
const ListActions = () => (
  <TopToolbar>
    <FilterButton />
    <CreateButton />
    <ExportButton />
    <SelectColumnsButton />
  </TopToolbar>
);
const ChatSettingsTitle = () => {
  const record = useRecordContext();
  return <span>ChatSettings {record ? `"${record.notifications}"` : ''}</span>;
};

export const ChatSettingsList = () => (
  <List actions={<ListActions />} filters={ResourceFilters}>
    <DatagridConfigurable>
      <BooleanField source="notifications" />
      <NumberField source="autoDelete" />
      <ReferenceField source="theme" reference="Themes" />
      <TextField source="languagePreference" />
      <EditButton />
    </DatagridConfigurable>
  </List>
);

export const ChatSettingsEdit = () => (
  <Edit title={<ChatSettingsTitle />}>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <BooleanInput source="notifications" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="autoDelete" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="theme" reference="Themes" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="languagePreference" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Edit>
);

export const ChatSettingsCreate = () => (
  <Create>
    <SimpleForm>
      <Grid container rowSpacing={1} columnSpacing={{ xs: 1, sm: 2, md: 5 }}>
        <Grid item xs={4}>
          <BooleanInput source="notifications" />
        </Grid>
        <Grid item xs={4}>
          <NumberInput source="autoDelete" />
        </Grid>
        <Grid item xs={4}>
          <ReferenceInput source="theme" reference="Themes" />
        </Grid>
        <Grid item xs={4}>
          <TextInput source="languagePreference" />
        </Grid>
      </Grid>
    </SimpleForm>
  </Create>
);

const ResourceFilters = [
  <TextInput source="q" label="Search" alwaysOn />,
  ,
  ,
  <ReferenceInput source="theme" label="theme" reference="Themes" alwaysOn />,
  ,
];
